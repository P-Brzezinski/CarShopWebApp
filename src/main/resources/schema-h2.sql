CREATE TABLE carsDB(id INT AUTO_INCREMENT,
                carBrand VARCHAR(20),
                carModel VARCHAR(20),
                color VARCHAR(20),
                yearOfProduction date,
                VIN VARCHAR(17),
                PRIMARY KEY (id));